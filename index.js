var myName = "charvi";
var age = 25;
console.log(myName);
//variable names are case sensitive
//variable name can start with underscrore or special character and $ but cannot start with number or cannot have space in betweeen them.
// no limit to length of variabble
//cannot use keywords.
console.l(age);